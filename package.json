{
  "name": "inhouse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Inhouse — AI app builder local (conductor + lovable) que usa o Claude Code da própria máquina",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "tsx watch server/index.ts",
    "dev:fake": "INHOUSE_FAKE_MODEL=1 INHOUSE_FAKE_DELAY_MS=400 INHOUSE_EVAL_A_CADA=1000000 INHOUSE_DATA_DIR=\"$HOME/.inhouse-debug\" INHOUSE_PROJECTS_DIR=\"$HOME/Inhouse-debug\" tsx watch server/index.ts",
    "start": "tsx server/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "debug": "tsx server/debug/runJourneys.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
